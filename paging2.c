# include <stdio.h>

int main()
{
    printf("Enter size of memory");
    scanf("%d",&ms);

    printf("Enter size of page")
    scanf("%d",&ps);

    nop=ms/ps;

    printf("number of available pages is %d",noap);

    printf("enter Number of processes");
    scanf("%d",&n);

    rem_pg=noap;

    for(i=0;i<n;i++)
    {
        printf("Enter pades reqd for process %d ",&p[i]);
    }

    if(p[i]>rem_pg)
    {
        printf("memory exceeded")
        break;
    }
    rem_pg-=p[i]
    printf("enter page table for process %d",i)
    for(i=0;i<p;i++)
    {
        for(j=0;j<n;j++)
            scanf("%d",&fno[i][j]);
    }
    printf("\nInvalid Process or Page Number or offset");

else
{ pa=fno[x][y]*ps+offset;
printf("\nThe Physical Address is -- %d",pa);

}
return 0;
}

